<Page
    x:Class="EngineLine.Pages.Connection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EngineLine.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ComboBox x:Name="protocolComboBox" Header="Protocol" PlaceholderText="Pick a protocol" Width="300" />
        <ComboBox x:Name="connectionMethodComboBox" Header="Connection Method" Width="300" SelectionChanged="ConnectionMethodComboBox_SelectionChanged"/>

        <StackPanel x:Name="serialConnectionOptions" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <ComboBox x:Name="comPortComboBox" Header="COM port" Width="300"/>
            <ComboBox x:Name="baudRateComboBox" Header="Baud rate" Width="300"/>
        </StackPanel>

        <Button x:Name="connectButton" Click="ConnectButton_Click" Margin="0,12,0,0">Connect</Button>
        <ProgressRing x:Name="connectProgressRing" IsActive="False" />
    </StackPanel>
</Page>
    